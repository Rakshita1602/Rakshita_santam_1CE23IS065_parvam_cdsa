#include <stdio.h>

int main() {
    int i = 1; // Initialize the loop counter
    while (i <= 5) {
        printf("%d\n", i); 
        i++; // Increment i to eventually stop the loop
    }

    return 0;
}