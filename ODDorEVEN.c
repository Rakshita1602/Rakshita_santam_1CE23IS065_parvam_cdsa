#include<stdio.h>

int main() {
    int n = 4956;

    // Condition to check for even number
    if (n % 2 == 0) {
        printf("%d is Even\n", n);
    }
    // Condition to check for odd number
    else {
        printf("%d is Odd\n", n);
    }

    return 0;
}
